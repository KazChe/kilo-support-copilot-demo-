# Runbook: <short symptom title>

## If you see this

- Error message contains: `<string>`
- HTTP status: `<code>`
- Log line matches: `<string>`
- Customer says something like: <one-line paraphrase>

## Confirm it's this

1. <step to verify the fingerprint>
2. <step to rule out look-alikes>

## Workaround

1.
2.
3.

## When to escalate

Escalate if any of the following are also true, because this may be a
different issue sharing surface-level symptoms:

- <condition>
- <condition>

## Related

- Ticket: <reference>
- Repro note: `artifacts/repro-note.md`
- Root cause: `artifacts/root-cause.md`
